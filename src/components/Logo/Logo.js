import React from 'react';
import datalinkLogo from '../../assets/images/logo.png';

const logo = (props) => (
    <div className="Logo">
        <a href="#"><img src={datalinkLogo} alt="datalink" /></a>
    </div>
);

export default logo;