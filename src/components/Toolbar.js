import React, { Component } from 'react';

const toolbar = (props) => (
    <header className="Toolbar">
        <div>Menu</div>
        <div>Logo</div>
        <nav>
            ...
        </nav>
    </header>
);

export default toolbar;
